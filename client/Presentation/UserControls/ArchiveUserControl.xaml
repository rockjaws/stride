<!--
  =============================================================================
  Author: Oliver
  =============================================================================
-->

<UserControl x:Class="client.Presentation.UserControls.ArchiveUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <UserControl.Resources>

        <Style x:Key="RestoreButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Background" Value="#16A34A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#15803D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">

                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="#15803D"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="#166534"/>
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DeleteButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Background" Value="#EF4444"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#DC2626"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">

                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="#DC2626"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="#B91C1C"/>
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid Background="#F1F5F9">

        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#F8FAFC"
                BorderBrush="#E2E8F0"
                BorderThickness="0,0,0,1">

            <StackPanel Margin="18,10">

                <TextBlock Text="Archived Projects"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="#0F172A"/>

                <TextBlock Text="{Binding ArchivedProjects.Count,
                                          StringFormat='{}{0} archived project(s)'}"
                           FontSize="12"
                           Foreground="#64748B"/>

            </StackPanel>

        </Border>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto">

            <ItemsControl ItemsSource="{Binding ArchivedProjects}">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Margin="12"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>

                    <DataTemplate>

                        <Border Width="320"
                                Margin="8"
                                Padding="14"
                                Background="White"
                                BorderBrush="#E2E8F0"
                                BorderThickness="1"
                                CornerRadius="8">

                            <StackPanel>

                                <Grid Margin="0,0,0,10">

                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{Binding Title}"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               Foreground="#0F172A"
                                               TextTrimming="CharacterEllipsis"
                                               VerticalAlignment="Center"/>

                                    <Border Grid.Column="1"
                                            Margin="8,0,0,0"
                                            Background="#FEF2F2"
                                            BorderBrush="#FECACA"
                                            BorderThickness="1"
                                            CornerRadius="12"
                                            Padding="8,2">

                                        <TextBlock Text="Archived"
                                                   FontSize="11"
                                                   FontWeight="SemiBold"
                                                   Foreground="#B91C1C"/>

                                    </Border>

                                </Grid>

                                <TextBlock Text="{Binding Description}"
                                           TextWrapping="Wrap"
                                           Foreground="#475569"
                                           Margin="0,0,0,12"
                                           MaxHeight="42"/>

                                <Separator Margin="0,0,0,12"/>

                                <StackPanel>

                                    <TextBlock Text="{Binding Tasks.Count,
                                                              StringFormat='Tasks: {0}'}"
                                               Foreground="#64748B"
                                               Margin="0,0,0,6"/>

                                    <TextBlock Text="{Binding Users.Count,
                                                              StringFormat='Members: {0}'}"
                                               Foreground="#64748B"
                                               Margin="0,0,0,6"/>

                                    <TextBlock Text="{Binding Deadline,
                                                              StringFormat='Deadline: {0:d}'}"
                                               Foreground="#64748B"/>

                                </StackPanel>

                                <Separator Margin="0,12"/>

                                <Grid>
                                    <Button Content="Restore"
                                            Style="{StaticResource RestoreButtonStyle}"
                                            Command="{Binding DataContext.RestoreProjectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            HorizontalAlignment="Left"/>

                                    <Button Content="Delete"
                                            Style="{StaticResource DeleteButtonStyle}"
                                            Command="{Binding DataContext.DeleteProjectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            HorizontalAlignment="Right"/>
                                </Grid>

                            </StackPanel>

                        </Border>

                    </DataTemplate>

                </ItemsControl.ItemTemplate>

            </ItemsControl>

        </ScrollViewer>

        <Border Grid.Row="1"
                Background="Transparent"
                IsHitTestVisible="False">

            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>

                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ArchivedProjects.Count}"
                                     Value="0">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center">

                <TextBlock Text="📦"
                           FontSize="42"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="No Archived Projects"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="#334155"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Archived projects will appear here."
                           Foreground="#64748B"
                           HorizontalAlignment="Center"/>

            </StackPanel>

        </Border>

    </Grid>
</UserControl>
