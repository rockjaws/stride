<Window x:Class="client.Presentation.Views.SelectedTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:client.Presentation.Views"
        mc:Ignorable="d"
        Title="Stride"
        Height="450"
        Width="520"
        MinHeight="420"
        MinWidth="480"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Task"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="0 0 0 18"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Text="Title"
                       VerticalAlignment="Center"
                       Margin="0 0 12 10"/>
            <TextBox Grid.Row="0"
                     Grid.Column="1"
                     Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0 0 0 10"/>

            <TextBlock Grid.Row="1"
                       Grid.Column="0"
                       Text="Description"
                       VerticalAlignment="Top"
                       Margin="0 4 12 10"/>
            <TextBox Grid.Row="1"
                     Grid.Column="1"
                     Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                     Height="90"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     Margin="0 0 0 10"/>

            <TextBlock Grid.Row="2"
                       Grid.Column="0"
                       Text="Start date"
                       VerticalAlignment="Center"
                       Margin="0 0 12 10"/>
            <DatePicker Grid.Row="2"
                        Grid.Column="1"
                        SelectedDate="{Binding StartDate, Mode=TwoWay}"
                        Margin="0 0 0 10"/>

            <TextBlock Grid.Row="3"
                       Grid.Column="0"
                       Text="Deadline"
                       VerticalAlignment="Center"
                       Margin="0 0 12 10"/>
            <DatePicker Grid.Row="3"
                        Grid.Column="1"
                        SelectedDate="{Binding Deadline, Mode=TwoWay}"
                        Margin="0 0 0 10"/>

            <TextBlock Grid.Row="4"
                       Grid.Column="0"
                       Text="Progress"
                       VerticalAlignment="Center"
                       Margin="0 0 12 10"/>
            <ComboBox Grid.Row="4"
                      Grid.Column="1"
                      ItemsSource="{Binding ProgressOptions}"
                      SelectedItem="{Binding Progress, Mode=TwoWay}"
                      Margin="0 0 0 10"/>

            <TextBlock Grid.Row="5"
                       Grid.Column="0"
                       Text="Priority"
                       VerticalAlignment="Center"
                       Margin="0 0 12 0"/>
            <ComboBox Grid.Row="5"
                      Grid.Column="1"
                      ItemsSource="{Binding PriorityOptions}"
                      SelectedItem="{Binding Priority, Mode=TwoWay}"/>
        </Grid>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0 20 0 0">
            <Button Content="Delete"
                    Width="90"
                    Height="30"
                    Margin="0 0 8 0"
                    Click="onDeleteTask"/>
            <Button Content="Cancel"
                    Width="90"
                    Height="30"
                    Margin="0 0 8 0"
                    IsCancel="True"
                    Click="onCancel"/>
            <Button Content="Update"
                    Width="90"
                    Height="30"
                    IsDefault="True"
                    Click="onUpdateTask"/>
        </StackPanel>
    </Grid>
</Window>
